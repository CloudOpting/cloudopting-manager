'use strict';

angular.module('cloudoptingApp').controller('ActivitiController',
		function(SERVICE, $scope, $state, $log, ActivitiService) {
			// need to know if need to do something to set the $scope.processId
	

		});